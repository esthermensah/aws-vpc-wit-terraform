provider "aws" {
  region = "us-east-1" # Replace with your desired region

#Specify the access key and secret key in your console or in the code 
  # access_key = "YOUR_ACCESS_KEY"
  # secret_key = "YOUR_SECRET_KEY"
}